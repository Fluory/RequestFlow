// Public API of the `requests` module: request aggregate (status machine follows with #7).
export { createRequest, findDuplicate, getRequest, listRequests, type NewRequest, type RequestRow } from "./repository";
