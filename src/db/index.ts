export { createDatabase, pingDatabase, type Database, type DatabaseHandle } from "./client";
// Migrations: import `runMigrations` from "@/db/migrate" (deploy step and test setup only – never
// bundled into the web app).
